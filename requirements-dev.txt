# Дев-зависимости вынесены отдельно — pip-audit и CI ставят именно их.
# Версии не пиним намертво, чтобы Dependabot мог апдейтить (CI-08).
pytest>=8
pytest-cov>=5
coverage>=7
ruff>=0.5
mypy>=1.10
bandit>=1.7
pip-audit>=2.7
